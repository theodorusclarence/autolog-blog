name: Refresh Feed
on: [push]
jobs:
  refresh-feed:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch API Data 📦
        uses: JamesIves/fetch-api-data-action@1.0.19
        with:
          ENDPOINT: https://theodorusclarence.com/api/content
          CONFIGURATION: '{ "method": "GET" }'

      - name: Check output
        run: |
          d=`ls fetch-api-data-action`
          echo $d
          echo $d > OUTPUT
